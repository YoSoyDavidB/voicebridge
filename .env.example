# ═══════════════════════════════════════════════════════════════
# VoiceBridge Configuration Template
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your actual API keys
# DO NOT commit .env to version control!


# ─────────────────────────────────────────────────────────────────
# REQUIRED API KEYS
# ─────────────────────────────────────────────────────────────────

# Deepgram API Key (Speech-to-Text)
# Get yours at: https://deepgram.com/
# Free tier: $200 credit
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# OpenAI API Key (Translation)
# Get yours at: https://platform.openai.com/
# Pay-as-you-go, GPT-4o-mini is very affordable
OPENAI_API_KEY=your_openai_api_key_here

# ElevenLabs API Key (Text-to-Speech with Voice Cloning)
# Get yours at: https://elevenlabs.io/
# Free: 10k chars/month, Creator ($5/month) for voice cloning
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# ElevenLabs Voice ID (Your cloned voice)
# Create your voice clone at: https://elevenlabs.io/voice-lab
# After cloning, copy the voice ID from the dashboard
ELEVENLABS_VOICE_ID=your_voice_id_here


# ─────────────────────────────────────────────────────────────────
# OPTIONAL CONFIGURATION (with sensible defaults)
# ─────────────────────────────────────────────────────────────────

# Audio Configuration
# AUDIO_INPUT_DEVICE_ID=        # Leave empty for system default
# AUDIO_OUTPUT_DEVICE_ID=       # Leave empty for system default
# AUDIO_SAMPLE_RATE=16000       # 16kHz is optimal for speech
# AUDIO_CHANNELS=1              # Mono audio
# AUDIO_CHUNK_DURATION_MS=30    # 30ms chunks for low latency

# Deepgram STT Configuration
# DEEPGRAM_LANGUAGE=es          # Spanish
# DEEPGRAM_MODEL=nova-2         # Nova-2 model (best quality/speed)

# OpenAI Translation Configuration
# OPENAI_MODEL=gpt-4o-mini      # Fast and affordable
# OPENAI_TEMPERATURE=0.3        # Low temperature for consistent translation

# ElevenLabs TTS Configuration
# ELEVENLABS_MODEL=eleven_turbo_v2_5  # Fastest model
# TTS_STABILITY=0.5                    # Voice stability (0-1)
# TTS_SIMILARITY_BOOST=0.8             # Voice similarity (0-1)
# TTS_OPTIMIZE_STREAMING_LATENCY=3     # Latency optimization (0-4)

# Voice Activity Detection (VAD)
# VAD_THRESHOLD=0.5                    # Speech detection threshold
# VAD_MIN_SPEECH_DURATION_MS=250       # Min speech duration
# VAD_MIN_SILENCE_DURATION_MS=300      # Min silence to end utterance
# VAD_SPEECH_PAD_MS=100                # Padding around speech
# VAD_MAX_UTTERANCE_DURATION_MS=15000  # Max utterance before force-split

# Pipeline Configuration
# PIPELINE_LOG_LEVEL=INFO              # DEBUG, INFO, WARNING, ERROR
# PIPELINE_METRICS_ENABLED=true        # Enable metrics collection

# Fallback Configuration
# FALLBACK_TTS_PROVIDER=openai         # Fallback if ElevenLabs fails
# FALLBACK_TTS_VOICE=onyx              # OpenAI voice for fallback
